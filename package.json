{
  "name": "smart-senior",
  "version": "0.1.0",
  "private": true,
  "description": "Smart Senior MVP — HTML/Tailwind/Alpine.js + Supabase on Cloudflare Pages",
  "scripts": {
    "deploy": "wrangler pages deploy . --project-name=smart-senior"
  }
}
