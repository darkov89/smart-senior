{
  "name": "smart-senior",
  "version": "0.1.0",
  "private": true,
  "description": "Pakiet Spokoju — Next.js /web on Cloudflare OpenNext; legacy static Pages",
  "scripts": {
    "web:dev": "npm run dev --prefix web",
    "web:preview": "npm run preview --prefix web",
    "deploy:legacy": "wrangler pages deploy . --project-name=smart-senior"
  }
}
