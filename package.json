{
  "name": "smart-senior",
  "version": "0.1.0",
  "private": true,
  "description": "Pakiet Spokoju — Next.js /web on Cloudflare Pages smart-senior",
  "scripts": {
    "web:dev": "npm run dev --prefix web",
    "web:preview": "npm run preview --prefix web",
    "deploy:web": "npm run deploy --prefix web"
  }
}
